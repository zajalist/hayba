[package]
name = "hayba-seeds"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Hierarchical deterministic seed derivation for the Hayba planetary sim. Master seed → child seeds per scale tier and coordinate, via SplitMix64. Bit-exact across architectures."

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
