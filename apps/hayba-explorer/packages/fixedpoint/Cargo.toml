[package]
name = "hayba-fixedpoint"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Q16.16 / Q31.32 fixed-point arithmetic + vec/mat/trig primitives. Bit-exact across architectures — the determinism foundation for the Hayba planetary sim stack."

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
