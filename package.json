{
  "name": "hayba",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered terrain and procedural generation — Gaea + PCGEx + Unreal Engine",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "license": "MIT"
}
