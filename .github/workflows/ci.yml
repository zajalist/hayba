name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  production-dependency-audit:
    name: Production dependency audit — reachability + expiry
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      # The audit needs the resolved lock graph, not lifecycle scripts.
      - name: Install without lifecycle scripts
        run: npm ci --ignore-scripts

      # The script invokes `npm audit --omit=dev --json` itself, parses raw JSON
      # in memory, and emits only sanitized package/advisory policy codes.
      - name: Reject unassessed or expired production risk
        run: npm run audit:production

  lint:
    name: TypeScript lint + format check (mcp-tools/hayba-mcp)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci --ignore-scripts

      # Scoped to the Node MCP server and non-blocking (like the sidecar ruff
      # step). The restructured tree carries large pre-existing subsystems that
      # predate this config and were never linted/formatted; enforcing repo-wide
      # or even subtree-wide would brick CI on code untouched by current work.
      # Kept as a visible signal until a formatting baseline is adopted.
      - name: Lint
        run: npx eslint mcp-tools/hayba-mcp || true

      - name: Format check
        run: npx prettier --check "mcp-tools/hayba-mcp/**/*.{ts,js,json,md}" --ignore-path .prettierignore || true

  node:
    name: Node MCP server — typecheck + build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install
        run: npm ci --ignore-scripts

      - name: Embedding fallback contract — cold cache + offline network
        working-directory: mcp-tools/hayba-mcp
        run: npx vitest run src/tools/routing/embedding-backends.test.ts src/tools/routing/tool-index.test.ts src/tools/production-dependency-audit-contract.test.ts

      - name: Typecheck
        working-directory: mcp-tools/hayba-mcp
        run: npx tsc --noEmit

      - name: Build
        working-directory: mcp-tools/hayba-mcp
        run: npx tsc

      - name: Test
        working-directory: mcp-tools/hayba-mcp
        run: |
          if [ -f vitest.config.ts ] || [ -f jest.config.ts ]; then
            npm test
          else
            echo "no test runner configured, skipping"
          fi

      # Separate step so a sidecar/cpp/wrapper drift shows up with its
      # own clearly-labeled failure in the GitHub UI instead of getting
      # buried in vitest output. The script is also exposed as a vitest
      # test (scripts/__tests__/check-legacy-wrappers.test.ts) so local
      # `npm test` runs surface the same drift.
      - name: Legacy command wrapper lint
        working-directory: mcp-tools/hayba-mcp
        run: npm run lint:legacy-wrappers

      # Fails only when a command declared in C++ is reachable by nothing --
      # no sidecar descriptor and no TS reference. Deliberate omissions are
      # allowlisted with reasons inside the script, and the check also fails
      # when an allowlist entry becomes reachable, so the list cannot rot.
      # Absence from sidecar.json alone is NOT drift: ~80 commands are reached
      # through hand-written TS wrappers instead, and gating on the sidecar
      # would fail on all of them until someone switched the gate off.
      - name: Capability inventory drift
        run: node tools/capability-inventory.mjs --check

      # Resources/Icons is generated from the signed masters. This proves the
      # committed rasters still match, so editing a master without rebuilding
      # cannot ship stale art. Hash-based, not mtime-based -- git does not
      # preserve modification times, so on a fresh checkout an mtime comparison
      # would decide by accident.
      - name: Icon rasters current with masters
        run: node tools/build-icons.mjs --check

      # A tool named in a system prompt or a workflow skill must exist. The
      # agent prompt used to instruct every model to "always call
      # hayba_generate_moodboard or hayba_fetch_references" at the start of a
      # new scene task; neither was ever implemented, so every such task opened
      # with a failed call. A dead catalogue entry is read when someone looks;
      # a dead prompt instruction is read every single time.
      - name: Prompts name only tools that exist
        run: node tools/prompt-tool-check.mjs --check

      # FHaybaMCPStyle::Colour returns MAGENTA for an unknown token, on purpose
      # -- on cool dark chrome a missing token returning black or white looks
      # like a design choice and ships unnoticed. But magenta is only loud to
      # someone who opens that panel, in that state. A typo'd token compiles,
      # links, runs, and is wrong.
      - name: Style tokens referenced actually exist
        run: node tools/style-token-check.mjs --check

      # tsc copies no data files, so the packaged server needs these copied
      # alongside it. The copy list used to be a one-line `node -e` in
      # package.json naming src/slivers/specs; renaming that directory broke
      # the build while every test stayed green, because vitest runs from src/
      # and never touches the packaged layout.
      - name: Build assets still exist where the build expects them
        run: node tools/copy-assets.mjs --check
        working-directory: mcp-tools/hayba-mcp

      # Docs and code can each be internally consistent and disagree with each
      # other -- a defect no test catches, because nothing reads both. The
      # README documented the MCP entry as `hayba-toolkit` while the tooling
      # wrote `hayba`, so `doctor` told everyone who followed the README that
      # their working install was unconfigured.
      - name: Docs name real CLI commands, and do not prescribe an entry name
        run: node tools/docs-command-check.mjs --check

      # GetCommands() is the advertisement -- it feeds the capability
      # inventory, CAPABILITIES.md and the router's dispatch table. Handle()
      # is the implementation. Nothing checked that the two agree, so a
      # command could be listed for agents, routed to its handler, and fall
      # through to that handler's own "unknown command" tail: advertised,
      # reachable, and broken, with the agent reasonably concluding the plugin
      # was out of date.
      - name: Declared commands are actually implemented
        run: node tools/declared-command-check.mjs --check

  architecture:
    name: Architecture — clean install + build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Clean install
        run: npm ci --ignore-scripts

      # The package test builds first, exercises the real demo server tests,
      # then runs the TypeScript suite. It also owns the CDN-only Three.js
      # boundary guard, so an inert npm dependency cannot drift back unnoticed.
      - name: Build and test
        run: npm test --workspace=@hayba/architecture

      - name: Typecheck
        run: npm run typecheck --workspace=@hayba/architecture

  sidecar:
    name: Visual sidecar — import + test + lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mcp-tools/hayba-mcp/addons/visual-embeddings
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      # Deliberately NOT installing torch. The sidecar's model loaders import
      # their heavy dependency inside the function that needs it, so the app
      # must start and answer /health with none of the weights present. A CI run
      # that installed torch would not be testing that property — it would be
      # hiding a regression in it.
      - name: Install sidecar (torch-free)
        run: |
          python -m pip install --upgrade pip
          pip install ruff
          pip install -e .[test]

      # This job was previously named "import + lint" and never imported
      # anything. Importing the app is the check: it was unimportable without
      # multi-GB weights for as long as the model modules imported at module
      # scope, which took /health down with them.
      - name: Import the app without weights
        run: python -c "from hayba_sidecar.server import app; print(sorted({r.path for r in app.routes}))"

      - name: Test
        run: pytest -q

      - name: Lint
        run: ruff check . || true # non-blocking until a style baseline is adopted
