# State marks — v4c (two-tone, detailed)

Same pipeline (image_gen → palette-normalize to flat `#DED4C3`/`#C47A28`,
transparent, 512px). TWO candidates each. Do not ask approval — execute.

## What was wrong before
Round 1 (triangle/clock-wedge/dot): read as notification badges. Round 2
(pennant/thin hourglass/thin spark): too thin, no silhouette. Round 3
(solid single-ochre flame/hourglass/drop): good mass but FLAT — single-color
blobs that look like stickers next to the two-tone icon set.

## This round: full two-tone craft
Each mark gets the same treatment as the main icons: a cream `#DED4C3` body
PLUS ochre `#C47A28` detail (or inverted), with real internal shaping —
carved bands, inner forms, layered elements — while keeping one strong
readable outer silhouette that holds at 14-16px. Think: the same folk
woodblock hand that made the save-floppy and the rules-banner.

1. **state-attention** — (a) the flame again, but two-tone: cream outer
   flame with a distinct ochre inner flame; (b) a rounded signal-horn /
   folk trumpet, cream body, ochre bell opening and a small sound wedge.
2. **state-pending** — (a) the hourglass with craft: cream frame with top
   and bottom caps, ochre sand mass sitting in the lower bulb and a falling
   ochre trickle; (b) a folk lantern: cream housing, ochre light kernel.
3. **state-unsaved** — (a) the ink drop with craft: cream drop body with an
   ochre core/highlight, or an ochre drop falling from a cream nib above;
   (b) a wax seal mid-press: ochre seal disc with a cream stamp handle
   still touching it (not yet set = not yet saved).

## Deliverables
6 PNGs + `contact-sheet.png` + one-line `NOTES.md` into
`docs/design/2026-08-23-extension-redesign/icons-v4c/`. Touch nothing else.
