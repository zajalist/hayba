world — a: the rising sun over a broad horizon reads most immediately as a place.
library — a: the three distinct book spines communicate a library fastest at small size.
rules — a: the ochre keystone makes the supporting-rule metaphor unusually clear.
chat — a: the overlapping bubbles communicate an exchange more strongly than a single bubble.
profile — a: the ID card is the most literal and compact profile metaphor.
undo — a: the clean U-turn arrow remains unmistakable at toolbar scale.
reject — a: the plain uniform X is the strongest and least decorative rejection mark.
save — a: the latched strongbox communicates something deliberately kept.
refresh — a: the two chasing arrows make the repeated cycle immediately legible.
connect — a: the centered docking joint clearly shows two halves becoming one.
build — a: the visible hammer-to-block impact conveys active construction.
terrain — a: the stepped mesa reads as land rather than as a generic layered object.
material — a: the two-tone checker tile is the clearest material-swatch convention.
python — b: the broad S-curve serpent survives reduction better than the compact coil.
camera-viewport — a: four isolated corner brackets are the cleanest viewport metaphor.
state-attention — a: the folk pennant is distinctive without resembling a notification badge.
state-pending — a: the hourglass remains recognizable as a tiny inline waiting mark.
state-unsaved — a: the Hayba spark is the most brand-native expression of something not yet settled.
