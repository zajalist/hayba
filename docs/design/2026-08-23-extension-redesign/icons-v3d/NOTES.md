save — the generator baked a checkerboard into the background; palette normalization removed it and restored transparent alpha.
python — the coiled form needed an explicit no-animal constraint to avoid literal snake features.
